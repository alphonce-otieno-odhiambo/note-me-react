import React, {useState, useEffect} from 'react'

const NotesList = () => {
    const [notes, setNotes] = useState([]);

    useEffect(() => {
      first
    
      return () => {
        second
      }
    }, [third]);

    const getNotes = asyn
    
  return (
    <div>
        nots
    </div>
  )
}

export default NotesList